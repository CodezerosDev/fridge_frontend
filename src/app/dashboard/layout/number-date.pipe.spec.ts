import { NumberDatePipe } from './number-date.pipe';

describe('NumberDatePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberDatePipe();
    expect(pipe).toBeTruthy();
  });
});
