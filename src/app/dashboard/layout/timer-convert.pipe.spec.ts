import { TimerConvertPipe } from './timer-convert.pipe';

describe('TimerConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new TimerConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
